# Submission README — DeepThought Target Company Research

Contents to upload to Internshala (GitHub link or Google Drive):

1) Part A deliverables (in this repo `outputs/`):
   - `master_company_database.xlsx` — full master dataset for the 25 profiled companies.  
   - `shortlisted_companies.xlsx` — companies marked as Pass.  
   - `borderline_companies.xlsx` — Borderline candidates requiring review.  
   - `rejected_companies.xlsx` — Fails and auto-disqualify examples.

2) Methodology and code:
   - `methodology.md` — research process, sources, and assumptions.  
   - `partB_1000_company_proposal.md` — sourcing strategy and scale-up plan.

3) Reports and raw notes (if used):
   - `reports/` — executive summary and analysis templates.  
   - `raw_research/` — LinkedIn, Crunchbase, hiring signals notes.

4) Hand-drawn diagram (mandatory for Part B):
   - Take a photo of your hand-drawn funnel/plan and upload it *directly* in the Internshala chat window.  
   - The photo file should be named: `yourname_1000_plan.jpg` and include a short caption with your name and date.

Submission checklist (what to paste in Internshala chat):
  - GitHub repo link or Google Drive folder link containing the files above.  
  - Confirm you have attached the hand-drawn diagram photo in the chat (required).

Notes on evidence:
  - Each Excel record contains a `Primary_Source_URL` column with the company website. For reviewer convenience, check the `Website` and `Primary_Source_URL` fields and open the links to verify per-criterion evidence.  
  - If reviewers request, I can expand every claim with direct per-criterion URLs (this requires extra scraping/time).

Contact
  - If you want me to produce the final GitHub repo zip (ready to push) or add full per-claim links inside the Excel files, tell me and I'll run the next step.
